node_modules/
dist/
*.log
.env
.env.local

# Build artifacts (Eve / Nitro / Vercel)
.output/
.vercel/
.eve/

# Local session/credential cache written by the CLI (FileSessionStore default dir)
.makroify/

# Never commit the captured HAR — it contains live credentials & tokens
*.har

# Eve runtime session/durability state (never commit)
.workflow-data/
